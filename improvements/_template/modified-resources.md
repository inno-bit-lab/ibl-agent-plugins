# Modified Resources

List canonical repository paths, one per bullet.

- `plugins/<plugin>/skills/<skill>/SKILL.md`

If candidate files are provided, preserve the same repo-relative path under
`candidate/`.

